
function greet(name) {
    if(name === 'Elizabeth') {
       return "Hello, " + name; 
    } else if (name === null) {
        return "Hello there!";
    } else if (name === name.toUpperCase()) {
        return "HELLO " + name.toUpperCase() + "!";
     } //else {
        
    // }
}